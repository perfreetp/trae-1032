## 1. 架构设计

```mermaid
graph TD
    subgraph "前端应用"
        A["React 18 + Vite"]
        B["React Router 路由管理"]
        C["TailwindCSS 样式"]
        D["Recharts 图表库"]
        E["Lucide React 图标"]
        F["Mock 数据层"]
    end
    subgraph "UI 组件层"
        G["布局组件 Layout"]
        H["业务组件 Cards/Tables/Forms"]
        I["通用组件 Buttons/Modals/Tabs"]
    end
    subgraph "页面层"
        J["工单总览 Dashboard"]
        K["渠道接入 Channel"]
        L["工单处理 Process"]
        M["旅客回访 Review"]
        N["服务标准 Standards"]
        O["整改跟踪 Rectify"]
        P["质量分析 Analysis"]
    end
    A --> B
    A --> C
    A --> D
    A --> E
    A --> F
    J --> G
    K --> G
    L --> G
    M --> G
    N --> G
    O --> G
    P --> G
    J --> H
    K --> H
    L --> H
    M --> H
    N --> H
    O --> H
    P --> H
    H --> I
```

## 2. 技术描述

- **前端框架**：React@18 + TypeScript
- **构建工具**：Vite@5
- **样式方案**：TailwindCSS@3
- **路由管理**：React Router DOM@6
- **图表库**：Recharts@2
- **图标库**：Lucide React
- **状态管理**：React 内置 useState/useContext（轻量应用无需额外状态库）
- **数据方案**：前端 Mock 数据 + localStorage 持久化（演示用途）
- **日期处理**：date-fns

## 3. 路由定义

| 路由路径 | 页面名称 | 说明 |
|----------|----------|------|
| / | 工单总览 | 默认首页，数据看板和工单概览 |
| /channel | 渠道接入 | 工单录入、多渠道接入管理 |
| /process | 工单处理 | 工单列表、分派、处理 |
| /review | 旅客回访 | 回访记录、满意度统计 |
| /standards | 服务标准 | 标准查询、知识库 |
| /rectify | 整改跟踪 | 整改任务、责任确认 |
| /analysis | 质量分析 | 热点排行、月度报告 |

## 4. 数据模型

### 4.1 实体关系

```mermaid
erDiagram
    WORK_ORDER ||--o{ PROCESS_LOG : has
    WORK_ORDER ||--o{ REVIEW : has
    WORK_ORDER ||--o{ RECTIFY_TASK : has
    WORK_ORDER }o--|| TRAIN : relates
    WORK_ORDER }o--|| STATION : relates
    WORK_ORDER }o--|| PROBLEM_CATEGORY : classifies
    WORK_ORDER }o--|| DEPARTMENT : assigned_to
    RECTIFY_TASK }o--|| DEPARTMENT : responsible
    
    WORK_ORDER {
        string id PK
        string title
        string channel
        string passenger_name
        string passenger_phone
        string train_no
        string station
        string category
        string urgency
        string status
        datetime create_time
        datetime deadline
        string content
        string attachment
    }
    
    PROCESS_LOG {
        string id PK
        string order_id FK
        string operator
        string action
        string content
        datetime create_time
    }
    
    REVIEW {
        string id PK
        string order_id FK
        int satisfaction
        string comment
        datetime review_time
        bool is_repeat
    }
    
    RECTIFY_TASK {
        string id PK
        string order_id FK
        string department
        string measure
        string status
        datetime deadline
        datetime complete_time
    }
    
    DEPARTMENT {
        string id PK
        string name
        string manager
    }
```

### 4.2 数据字典

工单状态：pending(待受理), assigned(已分派), processing(处理中), replied(已答复), closed(已关闭)
紧急程度：normal(一般), urgent(紧急), critical(特急)
工单渠道：hotline(热线), web(网页), wechat(微信), app(APP)
满意度：5(非常满意), 4(满意), 3(一般), 2(不满意), 1(非常不满意)

## 5. 目录结构

```
src/
├── components/          # 通用组件
│   ├── Layout/         # 布局组件
│   ├── common/         # 通用UI组件
│   └── business/       # 业务组件
├── pages/              # 页面组件
│   ├── Dashboard/
│   ├── Channel/
│   ├── Process/
│   ├── Review/
│   ├── Standards/
│   ├── Rectify/
│   └── Analysis/
├── data/               # Mock 数据
├── types/              # TypeScript 类型定义
├── utils/              # 工具函数
├── App.tsx
├── main.tsx
└── index.css
```
